# mycel_vera_risk/mycel_vera_risk_infra/helm/values-dev.yaml — does NOT ship
image:
  pullPolicy: Never

config:
  LOG_LEVEL: debug

artifacts:
  web:
    command: ["npx", "vite", "dev", "--host", "--port", "3002"]
