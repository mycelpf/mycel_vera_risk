# mycel_vera_risk/docker/.env
# Build-time variables only — consumed by docker build and Tilt.
# Runtime config lives in Helm values and K8s Secrets.

ROOT_MODULE_NAME=mycel_vera_risk
ROOT_MODULE_PATH=./mycel_vera_risk
MYCEL_CORE_PATH=mycel_core
SUBMODULES=
